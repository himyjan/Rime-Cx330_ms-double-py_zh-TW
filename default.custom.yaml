#全局使用者配置文件
#必需在這裡啟用之後build里面才會出現小鶴雙拼
patch:
  menu:
    page_size: 5
  schema_list:
    - schema: bopomofo_tw # 注音 繁體字
    - schema: luna_pinyin_tw # 朙月拼音 繁體字
    #  - schema: luna_pinyin_fluency   # 語句流
    #  - schema: double_pinyin        # 自然碼雙拼
    - schema: double_pinyin_mspy # 微軟雙拼
    - schema: double_pinyin_flypy # 小鶴雙拼
    - schema: wubi_pinyin # 五筆拼音混合輸入

    ascii_composer/good_old_caps_lock: true # capslock 鍵切換英文並輸出大寫
    ascii_composer/switch_key:
      Caps_Lock: commit_code
      Shift_L: noop
      Shift_R: noop
      Control_L: commit_code
      Control_R: commit_code

  #左右Shift鍵輸入時直接上屏並切換英文
  "ascii_composer/switch_key/Shift_L": commit_code
  "ascii_composer/switch_key/Shift_R": commit_code

  #以方括號 “[” 和 “]” 来換頁
  key_binder/bindings:
    - when: paging
      accept: bracketleft
      send: Page_Up
    - when: has_menu
      accept: bracketright
      send: Page_Down
    #Ctrl+句點切換ASCII標點
    - { accept: "Control+period", toggle: ascii_punct, when: always }
